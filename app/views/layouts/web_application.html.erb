<!DOCTYPE html>
<html lang="en">

<head>
   <meta charset="utf-8">
   <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1">
   <meta name="description" content="Clarity Insurance Claims Dashboard">
   <meta name="keywords" content="insurance, app, claims, processing, adjusting, claim, flood">
   <title>Clarity </title>
   <!-- =============== VENDOR STYLES ===============-->
   <%= stylesheet_link_tag 'app', media: 'all' %>
   <!-- =============== VIEW VENDOR STYLES ===============-->
   <%= stylesheet_link_tag "application", params[:controller], :media => "all" %>
   <!-- =============== APP STYLES ===============-->
   <%= csrf_meta_tags %>
</head>

<body>
   <div class="wrapper">
      <!-- top navbar-->
      <header class="topnavbar-wrapper">
         <%= render partial: "layouts/partials/topnavbar" %>
      </header>
      <!-- sidebar-->
      <aside class="aside">
         <%= render partial: "layouts/partials/sidebar" %>
      </aside>
      <!-- offsidebar-->
      <aside class="offsidebar hide">
         <%= render partial: "layouts/partials/offsidebar" %>
      </aside>
      <!-- Main section-->
      <section>
         <!-- Page content-->
         <div class="content-wrapper">
            <%= yield %>
         </div>
      </section>
      <!-- Page footer-->
      <footer>
         <%= render partial: "layouts/partials/footer" %>
      </footer>
   </div>
   <!-- =============== VENDOR SCRIPTS ===============-->

   <%= yield :body_area %>

</body>
<%= javascript_include_tag 'base' %>
<%= javascript_include_tag "application", params[:controller] %>

</html>
